#include <bits/stdc++.h>

using namespace std;

ostream& operator<<(ostream & os, const vector<int> &a) {
    for (auto i : a) {
        os << i << ' ';
    }
    return os;
}

string s;

int main() {
    s = 
    return 0;
}